---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart vllm
  ansible.builtin.systemd:
    name: "{{ vllm_service_name }}"
    state: restarted
  when: vllm_service_started | bool
